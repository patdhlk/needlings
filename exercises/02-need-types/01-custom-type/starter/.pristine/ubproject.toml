"$schema" = "https://ubcode.useblocks.com/ubproject.schema.json"

[needs]
id_required = true
build_json = true

[[needs.types]]
directive = "req"
title = "Requirement"
prefix = "REQ_"

# TODO: register a new type so `.. story::` is recognised. Add a
#       [[needs.types]] block with:
#         directive = "story"
#         title     = "User Story"
#         prefix    = "US_"
